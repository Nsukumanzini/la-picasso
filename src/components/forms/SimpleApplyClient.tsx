"use client";
import * as React from "react";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";
import { propertyRecords } from "../properties/propertiesData";

const schema = z.object({ firstName: z.string().min(2, "Enter your first name"), lastName: z.string().min(2, "Enter your last name"), email: z.string().email("Enter a valid email"), phone: z.string().min(9, "Enter a valid phone number"), nationalId: z.string().regex(/^\d{13}$/, "Enter your 13-digit SA ID"), property: z.string().min(1, "Choose a residence"), viewingWindow: z.string().min(1, "Choose a viewing time"), nsfasProof: z.any().refine((files) => files?.length > 0, "Upload your NSFAS proof") });
type Values = z.infer<typeof schema>;
const input = "mt-2 h-12 w-full rounded-xl border border-[#d9ccc3] bg-white px-4 text-base text-neutral-900 outline-none focus:border-picasso-brown focus:ring-2 focus:ring-picasso-light";
export default function SimpleApplyClient() { const [sent, setSent] = React.useState(false); const { register, handleSubmit, formState:{errors, isSubmitting} } = useForm<Values>({ resolver:zodResolver(schema) }); const onSubmit=()=>setSent(true); if(sent) return <div className="rounded-2xl border border-[#cfe3d3] bg-[#f3faf4] p-8"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#287343]">Application received</p><h2 className="mt-3 font-serif text-3xl text-neutral-900">Thanks — we have your details.</h2><p className="mt-3 max-w-xl text-base leading-7 text-neutral-600">Our team will contact you about your viewing and next steps. If you need help now, call +27 76 988 3928.</p></div>; return <form onSubmit={handleSubmit(onSubmit)} className="rounded-2xl border border-[#e8dfd8] bg-white p-5 shadow-sm sm:p-8"><div className="border-b border-[#eee6e0] pb-6"><p className="text-sm font-semibold text-neutral-900">1. Your details</p><p className="mt-1 text-sm text-neutral-600">Tell us how to reach you.</p><div className="mt-5 grid gap-5 sm:grid-cols-2">{([["firstName","First name"],["lastName","Last name"],["email","Email address"],["phone","Phone number"],["nationalId","SA ID number"]] as const).map(([name,label])=><label key={name} className={name==="nationalId"?"sm:col-span-2":""}>{label}<input {...register(name)} type={name==="email"?"email":"text"} className={input} />{errors[name]&&<span className="mt-1 block text-sm text-rose-600">{errors[name]?.message as string}</span>}</label>)}</div></div><div className="pt-6"><p className="text-sm font-semibold text-neutral-900">2. Choose a residence</p><p className="mt-1 text-sm text-neutral-600">You can change your choice later.</p><div className="mt-5 grid gap-5 sm:grid-cols-2"><label>Preferred residence<select {...register("property")} className={input}><option value="">Select a residence</option>{propertyRecords.map((property)=><option key={property.id} value={property.slug}>{property.name}</option>)}</select>{errors.property&&<span className="mt-1 block text-sm text-rose-600">{errors.property.message}</span>}</label><label>Preferred viewing<select {...register("viewingWindow")} className={input}><option value="">Select a time</option><option>Morning</option><option>Afternoon</option><option>Evening</option></select>{errors.viewingWindow&&<span className="mt-1 block text-sm text-rose-600">{errors.viewingWindow.message}</span>}</label><label className="sm:col-span-2">NSFAS proof<span className="mt-2 block text-sm text-neutral-600">Upload a PDF, photo or screenshot (max 10MB).</span><input {...register("nsfasProof")} type="file" accept="image/*,.pdf" className="mt-3 block w-full rounded-xl border border-dashed border-[#cdbfb5] bg-[#fbfaf8] p-4 text-sm" />{errors.nsfasProof&&<span className="mt-1 block text-sm text-rose-600">{errors.nsfasProof.message as string}</span>}</label></div></div><div className="mt-8 flex flex-col gap-3 border-t border-[#eee6e0] pt-6 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-neutral-500">We use these details only to process your enquiry.</p><button disabled={isSubmitting} className="rounded-full bg-picasso-brown px-6 py-3 text-sm font-semibold text-white disabled:opacity-60">Send application</button></div></form>; }
